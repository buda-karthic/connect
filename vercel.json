{
  "buildCommand": "cd connect && npm run build",
  "outputDirectory": "connect/dist",
  "installCommand": "cd connect && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
